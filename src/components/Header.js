import React from 'react';

const Header = (props) => {
	return (
		<div className="header">
			<div className="container text-center">
				<h2>Validar Web Check-in</h2>
			</div>
		</div>
	);
};

export default Header;